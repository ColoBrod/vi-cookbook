// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id          Int          @id @default(autoincrement())
  email       String       @unique
  name        String
  password    String
  recipes     Recipe[]
  collections Collection[]
}

model Product {
  id   Int    @id @default(autoincrement())
  uuid String @unique @default(uuid())
  slug String @unique
  name String

  calories Float @default(0)
  protein  Float @default(0)
  fat      Float @default(0)
  carbs    Float @default(0)
  fiber    Float @default(0)
  sugar    Float @default(0)

  mineralNa Float @default(0)
  mineralK  Float @default(0)
  mineralCa Float @default(0)
  mineralFe Float @default(0)
  mineralMg Float @default(0)
  mineralZn Float @default(0)
  mineralP  Float @default(0)
  mineralSe Float @default(0)

  vitaminA   Float @default(0)
  vitaminC   Float @default(0)
  vitaminD   Float @default(0)
  vitaminE   Float @default(0)
  vitaminK   Float @default(0)
  vitaminB1  Float @default(0)
  vitaminB2  Float @default(0)
  vitaminB3  Float @default(0)
  vitaminB6  Float @default(0)
  vitaminB9  Float @default(0)
  vitaminB12 Float @default(0)

  glycemicIndex Float @default(0)

  tags      Tag[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Recipe {
  id           Int                @id @default(autoincrement())
  uuid         String             @unique @default(uuid())
  slug         String             @unique
  name         String
  imagePath    String             @default("")
  image        Media?             @relation(fields: [imageId], references: [id])
  imageId      Int?               @unique
  description  String?            @db.Text
  instructions String?            @db.Text
  items        RecipeItem[]
  authorId     Int                @default(1)
  author       User               @relation(fields: [authorId], references: [id])
  tags         Tag[]
  collections  CollectionRecipe[]
  createdAt    DateTime           @default(now())
  updatedAt    DateTime           @updatedAt
}

model RecipeItem {
  id             Int            @id @default(autoincrement())
  recipeId       Int
  recipe         Recipe         @relation(fields: [recipeId], references: [id], onDelete: Cascade)
  ingredientUuid String
  ingredientType IngredientType
  amount         Float
  unit           Unit
}

enum IngredientType {
  PRODUCT
  RECIPE
}

enum Unit {
  G
  ML
  TSP
  TBSP
  PCS
}

model Tag {
  id       Int       @id @default(autoincrement())
  name     String    @unique
  products Product[]
  recipes  Recipe[]
  isRecipe Boolean   @default(true)
}

model Collection {
  id       Int                @id @default(autoincrement())
  name     String
  recipes  CollectionRecipe[]
  author   User               @relation(fields: [authorId], references: [id])
  authorId Int

  @@unique([authorId, name])
}

model CollectionRecipe {
  id           Int        @id @default(autoincrement())
  collection   Collection @relation(fields: [collectionId], references: [id])
  collectionId Int
  recipe       Recipe     @relation(fields: [recipeId], references: [id])
  recipeId     Int
  weight       Int

  @@unique([collectionId, recipeId])
}

model Media {
  id        Int      @id @default(autoincrement())
  uuid      String   @unique @default(uuid())
  slug      String   @unique
  path      String
  mimeType  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  recipe    Recipe?
}

// model Ingredient {
//   id        Int      @id @default(autoincrement())
//   recipeId  Int
//   recipe    Recipe   @relation(fields: [recipeId], references: [id], onDelete: Cascade)
//   productId Int?
//   product   Product? @relation(fields: [productId], references: [id])
//   amount    Float
//   unit      Unit
// }

// model Category {
//   id       Int        @id @default(autoincrement())
//   name     String
//   parentId Int?
//   parent   Category?  @relation("CategoryHierarchy", fields: [parentId], references: [id])
//   children Category[] @relation("CategoryHierarchy")
//   products Product[]
// }
